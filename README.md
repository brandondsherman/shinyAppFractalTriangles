# shinyAppFractalTriangles

Run app by running these two lines of code in an R session

```
library(shiny)
runGitHub("shinyAppFractalTriangles", "brandondsherman")
```

If you don't already have shiny, first run:

```
install.packages("shiny")
```
